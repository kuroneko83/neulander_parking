## Tarefa do ULTRAPLAN

<!-- Ex.: 2.3 — link para docs/ULTRAPLAN.md se aplicável. "N/A" se não for uma tarefa do plano. -->

## Resumo

<!-- O que mudou e por quê (1-3 bullets). -->

-

## Checklist

- [ ] `pnpm lint && pnpm typecheck && pnpm test` verdes localmente
- [ ] `pnpm test:int` verde localmente (quando o PR toca `apps/api`)
- [ ] Testes adicionados/atualizados para o comportamento novo
- [ ] Docs/ADR atualizados, se a decisão ou algum contrato (`packages/contracts`) mudou
- [ ] Sem segredo commitado; `.env.example` atualizado se uma variável nova foi introduzida
