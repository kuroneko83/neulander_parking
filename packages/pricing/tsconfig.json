{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@neulander/config/tsconfig/library.json",
  "compilerOptions": {
    "types": ["node"],
    // See packages/contracts/tsconfig.json for why these are overridden, not just inherited.
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "node_modules"]
}
